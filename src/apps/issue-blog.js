import { ROUTE_BLOG } from '../router/route.name'

const config = {
    name: 'issue-blog',
    title: 'Blog',
    icon: 'reader.svg',
    route: ROUTE_BLOG,
    dockLocation: 'center',
}

export default config
